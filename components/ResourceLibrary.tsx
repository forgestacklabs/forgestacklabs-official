import Link from "next/link";
import { resources } from "@/lib/seo-content";
export default function ResourceLibrary() { return <section id="resource-library" className="px-6 py-28"><div className="mx-auto max-w-7xl"><p className="mb-5 text-[10px] font-bold uppercase tracking-[.45em] text-[#8BA888]">Guides & whitepapers</p><h2 className="max-w-4xl text-4xl font-medium tracking-tight md:text-6xl">Technical material built for decisions.</h2><p className="mt-6 max-w-2xl leading-relaxed text-[#121212]/55">Read online or download a PDF copy for internal architecture and discovery discussions.</p><div className="mt-12 grid gap-5 md:grid-cols-2">{resources.map(item => <article key={item.slug} className="rounded-[2rem] border border-white/70 bg-white/50 p-8 shadow-[0_18px_60px_rgba(0,0,0,.06)]"><div className="mb-8 flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-[.3em] text-[#8BA888]">{item.kind}</span><span className="text-xs text-[#121212]/35">{item.readTime}</span></div><h3 className="text-2xl font-medium tracking-tight">{item.title}</h3><p className="mt-4 text-sm leading-relaxed text-[#121212]/55">{item.description}</p><div className="mt-7 flex gap-5"><Link href={`/resources/${item.slug}`} className="text-xs font-medium text-[#8BA888]">Read online →</Link><a href={`/resources/${item.slug}/download`} className="text-xs font-medium text-[#121212]/55">Download PDF ↓</a></div></article>)}</div></div></section>; }
